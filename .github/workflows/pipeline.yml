name: hello-world
on:
 push:
   branches: [ "grupo-lilas-ex" ]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v1       
      - name: List Files
        run: | 
          pwd
          ls -la
          cd java-api
          cd target
          ls -la

      
